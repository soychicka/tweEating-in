<h1>My recipe box</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Instructions</th>
    <th>User</th>
  </tr>

<% @recipes.each do |recipe| %>
  <tr>
    <td><%=h recipe.name %></td>
    <td><%=h recipe.instructions %></td>
    <td><%=h recipe.user_id %></td>
    <td><%= link_to 'Show', recipe %></td>
    <td><%= link_to 'Edit', edit_recipe_path(recipe) %></td>
    <td><%= link_to 'Destroy', recipe, :confirm => 'Are you sure?  I mean "DESTROYYYYYY" in the "I am Sparta" sense.  Gone.  Forever.   So... are you really sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New recipe', new_recipe_path %>