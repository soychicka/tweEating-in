<%= render :partial => 'sidebar_filters' %>
<h1>My recipe box</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Instructions</th>
    <th>User</th>
  </tr>

<% @recipes.each do |recipe| %>
  <tr>
    <td><%= link_to h(recipe.name), recipe %></td>
    <td><%=h recipe.instructions.empty? ? '' : (recipe.instructions.slice(0,120) + '...') %></td>
    <td><%=h recipe.user.login %></td>
    <td><%= link_to image_tag('/images/controls/edit.gif'), edit_recipe_path(recipe) %></td>
    <td><%= link_to image_tag('/images/controls/no.gif'), recipe, :confirm => 'Are you sure?  I mean "DESTROYYYYYY" in the "I am Sparta" sense.  Gone.  Forever.   So... are you really sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New recipe', new_recipe_path %>